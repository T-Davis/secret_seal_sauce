name: secret_seal_sauce
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  cloud_firestore: any
  equatable: ^2.0.0-nullsafety.1
  firebase_analytics: any
  firebase_core: any
  firebase_storage: any
  flutter:
    sdk: flutter
  flutter_bloc: ^7.0.0-nullsafety.0
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint:

flutter:
  uses-material-design: true
  assets:
    - assets/images/
