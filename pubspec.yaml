name: secret_seal_sauce
description: A website to engage the public on Monk Seal conservation

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  cloud_firestore:
  equatable:
  firebase_analytics:
  firebase_core:
  firebase_storage:
  flutter:
    sdk: flutter
  flutter_bloc: 
  flutter_svg:
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  lint:
  test:

flutter:
  uses-material-design: true
  assets:
    - assets/images/
